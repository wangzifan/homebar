# API Configuration

# Option 1: Use Mock Backend (no AWS required)
# Uncomment this line for local development without AWS
# VITE_USE_MOCK=true

# Option 2: Use Real AWS Backend
# Set to false (or comment out) to use real API
VITE_USE_MOCK=false
# Replace with your actual API Gateway endpoint after deployment
VITE_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/prod
