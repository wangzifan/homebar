{
  "name": "myhomebar-backend",
  "version": "1.0.0",
  "description": "Backend Lambda functions for MyHomeBar application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.680.0",
    "@aws-sdk/lib-dynamodb": "^3.680.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^3.0.1"
  }
}
